#!/usr/bin/env ruby

if directory = ARGV.first
  root_path = File.expand_path directory
else
  puts 'USAGE: centurion /some/ruby/app'
  exit
end

require 'centurion'
Centurion.new(root_path).meter(ARGV[4] || 'HEAD')
