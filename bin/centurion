#!/usr/bin/env ruby

if directory = ARGV.first
  root_path = File.expand_path directory
else
  puts 'USAGE: evaluate.rb /some/app'
  exit
end

require 'bundler'
Bundler.setup :default
require 'centurion'
Centurion.new(root_path).meter(ARGV[2] || 'HEAD')
